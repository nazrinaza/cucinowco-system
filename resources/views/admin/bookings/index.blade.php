<x-layouts.admin title="Bookings"><form class="admin-filters" method="get"><select name="status"><option value="">All statuses</option>@foreach(['pending','confirmed','in_progress','completed','cancelled'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str($status)->replace('_',' ')->title() }}</option>@endforeach</select><button type="submit">Filter</button></form><div class="booking-admin-list">@forelse($bookings as $booking)<article class="admin-card"><div class="booking-date">@if($booking->scheduled_start)<strong>{{ $booking->scheduled_start->format('d') }}</strong><span>{{ $booking->scheduled_start->format('M Y') }}</span><small>{{ $booking->scheduled_start->format('g:i A') }}</small>@else<strong>--</strong><span>Unscheduled</span>@endif</div><div class="booking-info"><span class="status status-{{ $booking->status }}">{{ str($booking->status)->replace('_',' ')->title() }}</span><h2>{{ $booking->customer->name }}</h2><p>{{ $booking->service?->name ?? 'Cleaning service' }}</p><small>{{ $booking->service_address }}</small></div><form class="booking-edit" method="post" action="{{ route('admin.bookings.update',$booking) }}">@csrf @method('patch')<select name="status">@foreach(['pending','confirmed','in_progress','completed','cancelled'] as $status)<option value="{{ $status }}" @selected($booking->status===$status)>{{ str($status)->replace('_',' ')->title() }}</option>@endforeach</select><select name="staff_id"><option value="">Unassigned</option>@foreach($staff as $member)<option value="{{ $member->id }}" @selected($booking->staff_id===$member->id)>{{ $member->name }}</option>@endforeach</select><input type="datetime-local" name="scheduled_start" value="{{ $booking->scheduled_start?->format('Y-m-d\TH:i') }}"><input type="datetime-local" name="scheduled_end" value="{{ $booking->scheduled_end?->format('Y-m-d\TH:i') }}"><button class="admin-button" type="submit">Save</button></form></article>@empty<div class="admin-card empty-cell">Bookings created from accepted quotes will appear here.</div>@endforelse</div><div class="pagination-wrap">{{ $bookings->links() }}</div></x-layouts.admin>
