<x-layouts.admin title="Subscribers"><form class="admin-filters" method="get"><input type="search" name="q" value="{{ request('q') }}" placeholder="Search email"><button>Search</button></form><section class="admin-card"><div class="table-wrap"><table><thead><tr><th>Subscriber</th><th>Status</th><th>Source</th><th>Joined</th></tr></thead><tbody>@forelse($subscribers as $subscriber)<tr><td><strong>{{ $subscriber->name ?: 'Newsletter subscriber' }}</strong><small>{{ $subscriber->email }}</small></td><td><span class="status status-{{ $subscriber->status }}">{{ ucfirst($subscriber->status) }}</span></td><td>{{ ucfirst($subscriber->source) }}</td><td>{{ $subscriber->subscribed_at?->format('d M Y') }}</td></tr>@empty<tr><td colspan="4" class="empty-cell">Website signups will appear here.</td></tr>@endforelse</tbody></table></div></section><div class="pagination-wrap">{{ $subscribers->links() }}</div></x-layouts.admin>
