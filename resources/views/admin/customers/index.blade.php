<x-layouts.admin title="Customers"><form class="admin-filters" method="get"><input type="search" name="q" value="{{ request('q') }}" placeholder="Search name, email or phone"><button type="submit">Search</button></form><section class="admin-card"><div class="table-wrap"><table><thead><tr><th>Customer</th><th>Contact</th><th>Type</th><th>Activity</th><th>Added</th></tr></thead><tbody>@forelse($customers as $customer)<tr><td><a href="{{ route('admin.customers.show',$customer) }}">{{ $customer->name }}</a><small>{{ $customer->company_name }}</small></td><td>{{ $customer->phone }}<small>{{ $customer->email }}</small></td><td>{{ ucfirst($customer->type) }}</td><td>{{ $customer->quotes_count }} quotes &middot; {{ $customer->bookings_count }} bookings</td><td>{{ $customer->created_at->format('d M Y') }}</td></tr>@empty<tr><td colspan="5" class="empty-cell">Customers are added from quote requests and direct bookings.</td></tr>@endforelse</tbody></table></div></section><div class="pagination-wrap">{{ $customers->links() }}</div></x-layouts.admin>
