<x-layouts.admin title="Invoices"><form class="admin-filters" method="get"><select name="status"><option value="">All statuses</option>@foreach(['draft','sent','partial','paid','overdue','cancelled'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select><button type="submit">Filter</button></form><section class="admin-card"><div class="table-wrap"><table><thead><tr><th>Invoice</th><th>Customer</th><th>Due</th><th>Status</th><th class="align-right">Paid</th><th class="align-right">Balance</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr><td><a href="{{ route('admin.invoices.show',$invoice) }}">{{ $invoice->invoice_number }}</a><small>{{ $invoice->issued_at?->format('d M Y') }}</small></td><td>{{ $invoice->customer->name }}</td><td>{{ $invoice->due_at?->format('d M Y') }}</td><td><span class="status status-{{ $invoice->status }}">{{ ucfirst($invoice->status) }}</span></td><td class="align-right">RM {{ number_format($invoice->amount_paid,2) }}</td><td class="align-right">RM {{ number_format($invoice->balance,2) }}</td></tr>@empty<tr><td colspan="6" class="empty-cell">Accepted quotes can be converted into invoices.</td></tr>@endforelse</tbody></table></div></section><div class="pagination-wrap">{{ $invoices->links() }}</div></x-layouts.admin>
